<?php
unset($data);

include(CORE_DATAS."/gpio/model.data");
$model = $data;
unset($data);

if($model == "Model B+"){
	for ($i=0; $i <= 31 ;$i++) { 
		$data[] = [
		"value" => $i,
		"text" => $i
		];
	}
}
else //Support all models
{
	for ($i=0; $i <= 14 ;$i++) { 
		$data[] = [
		"value" => $i,
		"text" => $i
		];
	}
}
//todo Manage JSON

?>