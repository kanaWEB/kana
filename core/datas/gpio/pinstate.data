<?php
#@# name:GPIO state | exemple:1/0 | gpio:gpio;select;gpio/pinslists
if(isset($data_link_json->gpio)){
	$gpio = $data_link_json->gpio;
}

if(isset($_["gpio"])){
	$gpio = $_["gpio"];
}

$pinstate_command = "/usr/local/bin/gpio read ".$gpio;
$data = trim(shell_exec($pinstate_command));

if(isset($data_link_json)){
	if($data){
		$data = t("ON");
	}
	else
	{
		$data = t("OFF");
	}
}
?>