<?php
$object_db = new Entity("Group");
$objects = $object_db->populate();
unset($data);
if($objects){
	foreach($objects as $object){
		$data[] = [
		"text" => $object["object_name"],
		"value" => $object["id"]
		];
	}

}
else{
	$data = false;
}

?>