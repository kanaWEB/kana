<?php

//Settings.php -> Scenario Redirector
$tpl->draw(CORE_VIEWS."grids/container/bordered_open");
//3th Menu Navbar generation
include(CORE_VIEWS."menu/scenario.view");

//If a tab is selected redirected to the correct form
if(isset($_["tab"])){
$tab = $_["tab"];

		switch ($tab) {
			//Electronics Tabs
			case "trigger": //If action object have different gpios
			include(CORE_FORMS."/settings/triggers/trigger.form");
			break;

			//Scenario Tabs
			case "scenario":
				include(CORE_FORMS."/settings/triggers/scenario.form");
			break;

			//Unknown Tab
			//default:
			//break;
		}
	}


?>