<?php

$form_name = "Groups";
$form = new Form($form_name);
$selected = false;
$form->input([
	"type" => "hidden",
	"id" => "nb_actions",
	"value" => 0
	]);

$form->input([
	"type" => "chainedselect_buttons",
	"id" => "actions",
	]);

//include(CORE_VIEWS."forms/form_default.view");

$list_all_objects = True;
include(CORE_DATAS."object.data");
$plugin_data = $data;

include(CORE_DATAS."actions.data");
var_dump($data);

include(CORE_DATAS."url/commands.data");
$action_data = $data;
//var_dump($action_data);
$form->input([
	"type" => "chainedselect_plugin",
	"options" => $plugin_data,
	"selected" => false,
	]);

$form->input([
	"type" => "chainedselect_actions",
	"options" => $action_data,
	"selected" => false,
	]);

$form->display($tpl);


