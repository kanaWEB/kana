<?php
$objects = Functions::getdir(USER_OBJECTS);

foreach($objects as $object_name){
	//$action_name = $object_name;
	$display_allactions = true;
	include(CORE_VIEWS."datatable/dbtable_actions.view");
	unset($table_headers);
	unset($table_data);
}
?>